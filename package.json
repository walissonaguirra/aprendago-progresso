{
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "rollup -c && tailwindcss -i input.css -o dist/style.css --minify",
    "watch:css": "tailwindcss -i input.css -o dist/style.css --watch"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^1.0.0",
    "html-minifier-terser": "^7.2.0",
    "rollup": "^4.60.1",
    "tailwindcss": "^3.4.0"
  }
}
